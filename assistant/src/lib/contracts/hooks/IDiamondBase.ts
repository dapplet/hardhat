
import { Falsy, Params, QueryParams, TransactionOptions, TypedFilter, useCall, useContractFunction, useLogs } from '@usedapp/core'
import { Contract, utils } from 'ethers'

import { IDiamondBase, IDiamondBase__factory } from '../types'
const IDiamondBaseInterface = new utils.Interface(IDiamondBase__factory.abi)


export const useIDiamondBase = {
  ,
  events: {
  
  }
}
